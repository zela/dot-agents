# <Stage N+> — Roadmap (lighter backlog)

> Deferred work, captured so the in-scope architecture stays compatible. Detail each item into its own session doc when it's picked up — don't over-plan speculative work.

## In-scope decisions that keep later stages cheap

<List choices made now that make deferred features easy later — e.g. "the chart already accepts an array of datasets, so overlay is UI-only". This is the payoff of designing for extension; record it so it isn't lost.>

- <decision → future feature it unblocks>

## Stage N+1 — <theme>

1. **<Feature>** — <one-line scope + where it slots in.>
2. **<Feature>** — <scope. Note any new data/contract work it needs.>

## Stage N+2 — <theme>  *(if/when <trigger>)*

1. **<Feature>** — <scope.>

## Where new work slots into the existing pipeline

<A note on how deferred features extend current modules without schema/contract breaks — reassures that today's design won't need a rewrite.>
